# Stock Report Feature Plan

เป้าหมาย: สร้างแท็บเมนูใหม่ "รายงานคลังพัสดุ" สำหรับสรุปยอด รับเข้า, ใช้ไป, คงเหลือ (เฉพาะของใหม่) โดยจัดกลุ่มตามหมวดหมู่ และสามารถ Export เป็น CSV ได้

## Proposed Changes

### 1. `backend/server.js`
- **[NEW]** สร้าง API Endpoint ใหม่: `GET /api/reports/stock`
  - ดึงข้อมูลพัสดุทั้งหมดจาก Collection `Stock`
  - ดึงข้อมูลประวัติจาก `StockTransaction`
  - คำนวณยอด `totalIn` (รับเข้า) จาก Transaction ประเภท `IN` และ `ADJUST` (+) ที่เป็นของใหม่
  - คำนวณยอด `totalOut` (เบิกออก) จาก Transaction ประเภท `OUT` และ `ADJUST` (-) ที่เป็นของใหม่
  - จัดโครงสร้างข้อมูล (Group) ตาม `category` (หมวดหมู่) -> `itemType` (พัสดุ) -> `size` (ไซส์)
  - แนบยอด `newStock` (คงเหลือปัจจุบัน) ส่งกลับไปให้ Frontend

### 2. `frontend/admin.html`
- **[MODIFY]** เพิ่มเมนูใน Sidebar: 
  - แท็บใหม่ชื่อ "📊 รายงานคลังพัสดุ" ต่อจาก "จัดการคลังพัสดุ"
- **[NEW]** สร้าง Container ใหม่ `<div id="content-stock-reports" class="admin-tab-content hidden">`
  - ใส่ Header หัวข้อรายงาน
  - ใส่ปุ่ม "Export CSV" 
  - ใส่โครงสร้างตาราง (Table) สำหรับแสดงผลข้อมูลแบบจัดกลุ่ม (หมวดหมู่ -> พัสดุ -> ไซส์)

### 3. `frontend/admin.js`
- **[MODIFY]** เพิ่มฟังก์ชัน `loadStockReport()` สำหรับเรียก API 
- **[MODIFY]** เพิ่มฟังก์ชัน `renderStockReport(data)` เพื่อนำข้อมูลมาสร้างแถวในตาราง HTML โดยให้มีการไล่ระดับสีหรือเว้นวรรคให้เห็นชัดเจนว่านี่คือ หมวดหมู่, พัสดุ, และไซส์
- **[MODIFY]** เพิ่มฟังก์ชัน `exportStockReportCSV()` เพื่อแปลงข้อมูล JSON ที่จัดกลุ่มแล้ว ให้อยู่ในรูปแบบบรรทัด CSV (`หมวดหมู่,พัสดุ,ไซส์,รับเข้า,เบิกออก,คงเหลือ`) แล้วดาวน์โหลด
- **[MODIFY]** เสียบคำสั่ง `loadStockReport()` เข้าไปในรอบการ `refreshData()` หรือเมื่อกดคลิกแท็บนี้

## Verification Plan
1. โหลดหน้า Admin เพื่อดูเมนูใหม่ใน Sidebar
2. คลิกเมนู "รายงานคลังพัสดุ" ระบบจะต้องแสดงตารางที่จัดกลุ่ม หมวดหมู่ > รายการ > ไซส์ พร้อมตัวเลข รับเข้า, ใช้ไป, คงเหลือ ได้ถูกต้อง
3. กดปุ่ม "Export CSV" และตรวจสอบไฟล์ที่ดาวน์โหลดมาว่ามีภาษาไทยที่สมบูรณ์และคอลัมน์ตรงตามตาราง
